---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}
  
inputs:
  - name: scripts_folder
run:
  path: /bin/sh
  args: ["./scripts_folder/task_show_uname.sh"]
