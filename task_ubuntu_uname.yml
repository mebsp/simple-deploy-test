---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}
  
inputs:
  - name: scripts_folder
run:
  path: /bin/sh
  args: ["git-resource/task_show_uname.sh"]
